<header id="main_menu_bg">
<nav class='navbar navbar-custom navbar-fixed-top'>
  <center>
    <tr>
      <% if user_signed_in? %>
        <th><%= link_to 'Home',:root %></th>
        <th><%= link_to 'Movie list', movies_path %></th>
      <% end %>
      <th><%= image_tag('Flogo.png') %></th>
      <% if user_signed_in? %>
        <th><%= link_to 'Add New Movie', new_movie_path %></th>
        <th><%= link_to 'Log out', destroy_user_session_path,:method => :delete %> </th>
      <% end %>
    </tr>
  </center>
</header>